// Supabase removed — stub kept so old imports don't break at the module level.
// All data now goes through /api/db. All auth through /api/auth.
export const supabase = null;
export const isSupabaseReady = false;
